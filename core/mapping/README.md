# Mapping

This directory contains the declarative mapping plans and helper transforms used
by the tagging pipeline.

- `movies/` and `tv/`: TMDb  iTunes plan JSON and schema files.
- `transforms.py`: Helper functions referenced by plan rules.
- `genres.py`: Canonical genre mapping + normalization helpers.
